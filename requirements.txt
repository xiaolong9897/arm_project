# MuJoCo 物理引擎
mujoco>=3.0.0

# 数值计算
numpy>=1.20.0
scipy>=1.7.0

# 可视化
matplotlib>=3.3.0

# 计算机视觉
opencv-python>=4.5.0

# 机器人动力学
pin>=2.6.0

# 配置文件
pyyaml>=5.4.0

# MCAP 数据格式 (用于 rosbag 录制)
mcap>=0.0.10
mcap-ros2-support

# 注意：ROS2 相关包需要通过系统包管理器安装
# Ubuntu/Debian (ROS2 Humble):
# sudo apt install ros-humble-rclpy ros-humble-sensor-msgs ros-humble-geometry-msgs ros-humble-std-msgs ros-humble-cv-bridge ros-humble-rosbag2 ros-humble-rosbag2-storage-mcap
# 安装后需要: source /opt/ros/humble/setup.bash
